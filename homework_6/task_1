#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>


int func(const int a) {
    int prod = 1;
    for (int i = 1; i <= a; i++) {
        prod *= i;
    }
    return prod;
}

void main(){
    int number;
    scanf("%d", &number);
    printf("%d", func(number));
}
