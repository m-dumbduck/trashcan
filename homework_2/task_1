#include <stdio.h>
#include <math.h>

int main() {
	double a, b, hypo;
	printf("Введите катеты: ");
	scanf("%lf %lf", &a, &b);
	hypo = sqrt(a*a + b*b);
	printf("Гипотенуза: %lf\nПериметр: %lf\nПлощадь: %lf",
		hypo, a + b + hypo, a*b/2);
}
